.side-bar
  .side-bar__user
    .side-bar__user__name
      .side-bar__user__name__text
        = current_user.name
      =link_to new_group_path() do
        = icon('fas', 'edit')
      =link_to edit_user_path(current_user) do
        = icon('fas', 'cog')
  .side-bar__group-list
    - current_user.groups.each do |group|
      .side-bar__group-list__box
        = link_to '#' do
          .side-bar__group-list__box__groupname
            = group.name
          .side-bar__group-list__box__messege
            メッセージはまだありません。